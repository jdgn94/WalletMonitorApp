CREATE TABLE currency_types (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL
);

insertCurrencyType:
INSERT INTO currency_types(name) VALUES (?);

selectAllCurrencyTypes:
SELECT * FROM currency_types;

selectTypeByName:
SELECT * FROM currency_types WHERE name = ?;
