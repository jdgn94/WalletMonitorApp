CREATE TABLE currency_type (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL
);

insertCurrencyType:
INSERT INTO currency_type(name) VALUES (?);

selectAllCurrencyTypes:
SELECT * FROM currency_type;

selectTypeByName:
SELECT * FROM currency_type WHERE name = ?;
