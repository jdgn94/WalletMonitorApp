CREATE TABLE countries (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    abbr TEXT NOT NULL
);

insertCountry:
INSERT INTO countries(name, abbr) VALUES (?, ?);

selectAllCountries:
SELECT * FROM countries;
